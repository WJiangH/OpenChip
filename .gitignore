# simulation artifacts
obj_dir/
sim_build/
*.vcd
*.fst
*.fst.hier
*.vvp
results.xml
coverage.dat
annotate/

# formal artifacts
hw/formal/**/*_bmc/
hw/formal/**/*_prove/
hw/formal/**/*_cover/

# physical design runs (signoff summaries ARE committed, runs are not)
hw/pd/**/runs/

# firmware build
sw/**/*.o
sw/**/*.elf
sw/**/*.bin
sw/**/*.map

# python
__pycache__/
*.pyc
.venv/

# local toolchain cache (fetched RISC-V GCC, not a repo artifact)
.toolcache/

# editors / os
.DS_Store
.vscode/
.idea/

# codegraph cache
.codegraph/

# python coverage data (cocotb user-coverage residue)
.coverage

# sby state db
hw/formal/**/status.sqlite

# per-CLI skill entrypoints generated by tools/agents.sh (canonical: .agents/skills/).
# .claude/skills is committed — it is the reference CLI and must work on a bare clone.
.cursor/
.antigravitycli/
.grok/
.qwen/
.opencode/
.kimi/

# B1 interface artifacts: the SW->ROM-RTL and SW->DV bytes must travel with the branch (FLOW-003 / integrator F-09)
!sw/rom/rom.bin
!sw/b1_extmem.bin
!sw/b1_extmem_bad_magic.bin
!sw/b1_extmem_bad_crc.bin
