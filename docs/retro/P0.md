# P0 tracer-bullet retro — blink

Date: 2026-07-24 · Fleet: chief-architect/verif-architect/rtl/dv (Sonnet5-high),
formal/backend (Opus5-high), orchestrator (Fable5)

## Result

Full pipeline green from a cold machine in one day:
spec (8 shalls) → vplan+golden (21-check self-test) → RTL (lint 0 warn) →
cocotb 7/7 + 100%/100% line/toggle → k-induction unbounded proof (0 assumes,
mutation-tested) → synth 106 cells → GDS: DRC 0, LVS match, antenna 0,
setup +11.38 ns @ 20 ns. Zero RTL bugs — plausible for a 15-line design; the
pipeline, not the design, was under test.

## What proved to be fixed workflow (now codified)

- Role dispatch prompt skeleton: identity + read-your-SKILL + clean-room
  deny-list + task + verify-command + return-format. (docs/AGENTS.md material
  for the GitHub-routing phase.)
- Report format: per-shall traceability mapping + pasted tool output +
  tooling-friction list. Friction list is now mandatory (CLAUDE.md).
- Clean room held by prompt alone (L1): DV never read RTL, RTL never read
  hw/dv/, boundary-respecting workarounds observed twice (dv → flow defect
  reported not fixed; formal → .gitignore respected via naming).

## Distilled into the skill library (6 entries)

cocotb-2x-pitfalls (dv) · yosys-formal-pitfalls (formal, two silent-fake-proof
traps) · librelane-notes (backend, exit-0≠clean) · verilator-lint-pitfalls
(rtl) · golden-model-patterns (verif) · spec-authoring-patterns (architect).
Flow fixes landed directly: COVERAGE env collision, C++ std flag, COCOTB_*
migration, Apple Make 3.81 PATH, PDK cache mount, image digest pin.

## Open items

1. GL-sim (post-layout netlist + SDF re-run of the DV suite) not run — M4
   obligation, recorded in hw/pd/blink/SIGNOFF.md.
2. Branch/PR flow not rehearsed (work landed on main with post-hoc author
   signatures); real PR + integrator review cycle lands with GitHub routing.
3. GitHub issue routing (role labels, @-mention relay) not yet wired.
4. `make gds` currently assumes the P0 host's ~/.ciel cache; CI needs a
   cached-PDK strategy before the gds gate can run in Actions.
