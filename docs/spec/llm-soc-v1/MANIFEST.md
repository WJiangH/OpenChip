# Architecture deliverable manifest

Version1.0-rc4 · chief-architect · LOCAL ONLY · proposal branch `spec/llm-soc-v1-rc4`. Change order rc4 rules the 28 issue ids filed while implementing rc3 and is amended after the RTL-role (request-changes) and SW-role (approve-with-editorial) reviews of it (R4-01..R4-12, SW-RC4-01/02; disposition table at the end of CHANGE_ORDER_rc4.md); the version string is unchanged and the verification-role review is still pending. It becomes the baseline only after rc3-reviewer (RTL/verification/software) confirmation. No frozen/implemented-chip claim. Per-file scope/hash in MANIFEST.json. Forty-seven requirement IDs:44SIM-L1 and3S1 integration obligations (rc4 adds NPU-09 partition lifecycle and SYS-13 native port). Sixteen blocks include the excluded-from-SIM-L1 Caliptra candidate;48connections (rc4 adds C34 dot_lifecycle),14address regions,33CSR words and19AXI-Lite signals are enumerated.

Artifacts: README/system/AXI/NPU/software/security/dependency contracts; machine maps/connectivity/CSR/IRQ/requirements with per-protocol semantics; requirement traceability; downstream deliverables; ADR0004 migration (unchanged in rc4); resource script/JSON/report; fixed workload/profile and twoB1fixtures; reviewed software/IP intake; independent review02 reports; rc2/rc3/rc4 change orders and prior/review hashes; input/manifest/audit ledgers. No product implementation was authored. inputs-rc4/ (issue files, integrator review, and the rc4 RTL/SW review files rc4_review_rtl.md / rc4_review_sw.md) is committed on the proposal branch as the ruling input so the record travels with the branch; it is not hashed as an architecture deliverable (the two review files' sha256 are pinned in the change order's disposition table). workload.md is untouched by rc4 and keeps its 1.0-rc3 header by the house rule that a change order bumps only the files it edits.

Actual architect-executed summaries (rc4 amended, this worktree):

```text
BUDGET PASS: MAC/forward=259328 groups/forward=4152 payload=278752 KV512=655360 traffic_floor=278780
CONTRACT AUDIT PASS: blocks=16 connections=48 regions=14 CSR=33 requirements=47 Lite_signals=19; budget/profile consistent
```

Both commands exit0; they check architecture arithmetic and metadata consistency only (`python3 explore/llm-soc-v1/budget.py`, `python3 explore/llm-soc-v1/check_contract.py`). explore/llm-soc-v1/AUDIT.txt and AUDIT.json remain the rc3 records because this order commits only docs/spec/llm-soc-v1/; the rc4 run is recorded in MANIFEST.json executed_checks. MANIFEST.json hashes were computed with `shasum -a 256` (Python hashlib cross-check) because no manifest generator exists in this tree. No make hardware gate was run for these spec-only changes. Imported software/IP results are identified by owner in dependencies/workload reports; failing strict IP lint is preserved, generic synth is not area/timing acceptance, host inference is not DUT execution.

Open release/evidence items: rc3-reviewer confirmation of change order rc4; RTL changes named in CHANGE_ORDER_rc4.md (npu_ctl, npu_dot, llm_soc_top, sys, uart, fabric); independent DV closure of B1 against rc4; final RV32 soft-float/library/startup/link size/stack feasibility and execution; ISA/formal; S1 exact Caliptra supplement; P1 quality/live-tensor/calibrated performance/physical choices; FPGA board and actual memory controller. No threshold is waived and no missing hardware result is called passed.

- Friction: no manifest generator in the tree; check_contract.py globs every top-level *.md for bold requirement IDs, so change orders must not use the `**ID:**` form; dma_terminal/group_result/dispatch/native had no semantics field, which produced four of the 27 issues.
- Skill candidates: chief-architect/references/spec-authoring-patterns.md — Every internal protocol in the machine contract needs a semantics entry (handshake or level, who sets/clears, flush/abort path, ordering between sibling handshakes) and every stated register value range needs its rejection sentence, before handoff.

rc4 correction scope: 28 issue ids ruled: the 25 unique inputs-rc4/INDEX.md ids (26 rows, one duplicate) plus ISSUE-top-01 and ISSUE-sw_b1-01/02, which INDEX omits; the integrator's F-01/F-04/F-05/F-06/F-07 are ruled inside those rows. 16 spec-gap (two new requirement IDs), 8 spec-clear, 3 metadata, 1 rule-level; both audit commands rerun exit0 after the amendment. Await rc3-reviewer confirmation before baseline. No target or hardware validation is implied.
